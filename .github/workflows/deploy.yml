name: Build & Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    uses: mark-baumann/deployment-pipeline/.github/workflows/build-deploy.yml@main
    with:
      service_name: browser-nutzung
      port: 8515
    secrets: inherit
